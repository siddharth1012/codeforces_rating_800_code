#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;
    string str1;
    cin >> str1;
    int count = 0;
    
    for(int i = 0; i < n-1; i++)
        if(str1[i] == str1[i+1])
            count++;
    
    cout << count;
    return 0;
}
