#include<bits/stdc++.h>
using namespace std;

int main()
{
    int count = 0, n, p, q, r;
    cin >> n;
    
    for(int i = 0; i < n; i++)
    {
        cin >> p >> q >> r;
        if((p+q+r) >= 2)
            count++;
    }
    cout << count;
    return 0;
}
